#include <iostream>
#include <iomanip>
#include <list>
#include <vector>
#include <algorithm> // STL Algorithms
using namespace std;

#include "TemplateFunction.h"
#include "DivisibleBy.h"

int main() {
	
	// Uncomment when we look at <algorithm>
	
	list<int> mylist;
	mylist.push_back(201);
	mylist.push_back(10);
	mylist.push_back(101);
	mylist.push_back(150);
	mylist.push_back(22);
	mylist.push_back(8);
	mylist.push_back(1231);
	mylist.push_back(4319);
	mylist.push_back(314);
	

}